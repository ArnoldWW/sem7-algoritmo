# Solucion

## Primera tripla

true ∧ A >= 0 => (x = |A|) (x := A)
true ∧ A >= 0 => A = |A|

## Segunda tripla

true ∧ A < 0 => (x = |A|) (x := -A)
true ∧ A < 0 => (x = |A|) (x := -(-A))
true ∧ A < 0 => (x = |A|) (x := A)
true ∧ A < 0 => A = |A|
